#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints an alphabet
 * Return: 0 when successfully executed
 */

int main(void)
{
char lcase = 'a';

while (lcase <= 'z')
{
putchar(lcase);
lcase++;
}
putchar('\n');
return (0);
}
