#include "main.h"

/**
 * main - Main function here
 * Return: Always 0 when successful
 */

int main(void)
{
print_alphabet(
char letter = 'a';
while (letter <= 'z')
{
_putchar(letter);
letter++;
}
_putchar('\n');
);
return (0);
}
